# Core
pandas>=2.2.0
numpy>=1.26.0
pyyaml>=6.0.1
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2024.1

# Broker APIs
smartapi-python>=1.3.0
# smartapi-python imports logzero at runtime but does not declare it in its
# install_requires (upstream bug). Pin it explicitly so the test scripts and
# the live trading path (main.connect_angelone -> SmartApi.SmartConnect) both
# import cleanly inside a fresh container.
logzero>=1.7.0
pyotp>=2.9.0
websocket-client>=1.7.0

# Market data
yfinance>=0.2.36

# Technical indicators (used by feature engine)
ta>=0.11.0

# Logging
loguru>=0.7.2

# CLI dashboard
rich>=13.7.0
tabulate>=0.9.0

# Process management (used by stop_daemon.py)
psutil>=5.9.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Email alerts
resend>=0.7.0

# ML / training
xgboost>=2.0.0
scikit-learn>=1.4.0

# Optional dashboards (uncomment if used)
# streamlit>=1.30.0
# torch>=2.1.0

# NOTE: Kite Connect (kiteconnect) was deprecated 2026-05-08; AngelOne is the
# canonical broker -- see docs/journal/engineering_journal_2026-05-08.md.
