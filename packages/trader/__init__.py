"""POD 1: trader -- the live trading agent.

Imports allowed:  core, strategies, brokers
Imports FORBIDDEN: research, ui, training

Entry point (target):  python -m trader

Currently a SKELETON. The actual TradingAgent class lives at
`trading_agent.py` (project root) until the Phase 1.5 split. After that it
moves to `packages/trader/agent.py` along with the cycle/exits/safety/etc.
modules described in `docs/cloud_pod_architecture.md`.
"""
