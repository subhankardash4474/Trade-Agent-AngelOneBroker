# Findings — Full-codebase re-audit 2026-05-27

Generated by six parallel deep-dive read-only audits across:
- Orchestrator + entry points (`trading_agent.py`, `main.py`, `run_daemon.py`, `stop_daemon.py`, `conftest.py`)
- Broker + execution + risk + DB + secrets
- Strategies + features + regime + ensemble
- Data layer + monitoring + persistence + alerts
- Research/backtest + training pipelines
- Operator tools + deploy scripts + config

Items already fixed in `docs/changes/changes_done_2026-05-26.md` are EXCLUDED. Numbering is project-wide `F-NN`, grouped by subsystem prefix in the title.

Baseline: 1556 tests pass, ruff 58 bug-class, bandit 1 HIGH + 10 MEDIUM, no current crashes.

---

## CRITICAL (must-fix for live cutover, may require user policy input)

| ID    | Title                                                                                       | File                                                                                  |
| ----- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| F-01  | [orch] Live entry/exit treats broker `PLACED` ack as filled; portfolio drift risk           | `trading_agent.py`, `packages/core/execution.py`, `packages/brokers/angelone.py`      |
| F-02  | [orch] Emergency-STOP from WS / fast-exits short-circuits alert + flatten handler           | `trading_agent.py`                                                                    |
| F-03  | [orch] `_shutdown()` unconditionally flattens even when STOP requested without flatten flag | `trading_agent.py`                                                                    |
| F-04  | [ops]  Supervisor / Stage-3 / preflight watch `EMERGENCY_STOP` while daemon honours `logs/STOP` | `tools/run_daemon_resilient.ps1`, `tools/_preflight.py`, `tools/cloud/stage3_launch.sh` |

## HIGH

| ID    | Title                                                                                       | File                                                                                  |
| ----- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| F-05  | [orch] `_get_previous_close()` calls `get_historical_data` with invalid kwargs → guard silently disabled | `trading_agent.py`                                                                   |
| F-06  | [orch] `_resolve_starting_balance()` reads non-existent `execution.mode` instead of `broker.mode`        | `trading_agent.py`                                                                   |
| F-07  | [orch] JWT refresh updates REST clients but not WebSocket auth                              | `trading_agent.py`, `packages/core/websocket_client.py`                              |
| F-08  | [orch] STOP from WS does not abort an in-flight `_trading_cycle`                            | `trading_agent.py`                                                                    |
| F-09  | [orch] Exit accepts `FILLED`/`PLACED` but rejects `PARTIALLY_FILLED` (entry accepts it)      | `trading_agent.py`                                                                    |
| F-10  | [data] WebSocket `volume` field is cumulative session volume, not per-tick (candles wrong)   | `packages/core/websocket_client.py`, `packages/core/tick_aggregator.py`              |
| F-11  | [data] WS subscription changes not applied to live connection until reconnect                | `packages/core/websocket_client.py`                                                  |
| F-12  | [data] Regime-level accumulators not rehydrated from DB → PF=0 after every restart           | `packages/core/trade_analyzer.py`                                                    |
| F-13  | [strat] LSTM `set_market_context` stores ctx but `generate_signal` never passes it          | `packages/strategies/lstm_model.py`                                                  |
| F-14  | [strat] LSTM silently `fillna(0)` on inference but training drops NaN rows (train/serve skew) | `packages/strategies/lstm_model.py`                                                  |
| F-15  | [strat] LSTM accepts model load even when scaler missing → arbitrary predictions             | `packages/strategies/lstm_model.py`                                                  |
| F-16  | [brk]  `cancel_order` treats any truthy response as success (orphan SL-M on close)           | `packages/core/execution.py`, `packages/brokers/angelone.py`                         |
| F-17  | [tool] `run_battery_queue` treats vanished `--rm` container as exit code 0 → false success   | `tools/run_battery_queue.py`                                                         |
| F-18  | [tool] `_protective_close` seeds Portfolio from non-existent config keys                    | `tools/_protective_close.py`                                                          |
| F-19  | [tool] Six tools `from core.*` without `packages/` sys.path bootstrap → ModuleNotFoundError on host | `tools/replay_failed_alerts.py`, `_resend_eod.py`, `_resend_postmortem.py`, `angelone_smoke.py`, `angelone_login.py`, `_protective_close.py` |
| F-20  | [tool] `reconcile_trade_book.py --date` silently drops broker rows for historical days       | `tools/reconcile_trade_book.py`                                                       |
| F-21  | [train] LSTM `create_sequences()` slides window across symbol boundaries                    | `packages/training/train_lstm.py`                                                    |
| F-22  | [train] XGBoost `early_stopping_rounds` peeks at the official test set                       | `packages/training/train_xgboost.py`                                                 |
| F-23  | [train] LSTM ships final-epoch weights to `models/lstm_model.pt`; best checkpoint unused     | `packages/training/train_lstm.py`                                                    |
| F-24  | [train] Intraday training bars tagged with that-day Nifty/VIX *daily close* (lookahead)      | `packages/training/prepare_dataset.py`                                               |
| F-25  | [train] Battery ensemble backtest hardcodes `regime="unknown"`                              | `packages/research/backtest_ensemble.py`                                             |
| F-26  | [train] Backtest fill model never uses paper RNG; C-21 wiring incomplete                    | `packages/research/backtest_ensemble.py`                                             |

## MEDIUM

| ID    | Title                                                                                       | File                                                                                  |
| ----- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| F-27  | [orch] TrailingStop state mutated outside `_exit_check_lock` from WS thread                 | `trading_agent.py`, `packages/core/risk_manager.py`                                  |
| F-28  | [orch] `Portfolio.positions` read from WS path without `_pos_lock`                          | `trading_agent.py`, `packages/core/portfolio.py`                                     |
| F-29  | [orch] `_fast_exits_sleep` empty-book branch sleeps full interval w/o STOP polling          | `trading_agent.py`                                                                    |
| F-30  | [orch] `_resolve_starting_balance()` staleness uses naive `datetime.now()` (off-by-day on UTC VM) | `trading_agent.py`                                                              |
| F-31  | [orch] `main.py` / `run_daemon.py` paper-mode set chain-subscripts `config["broker"]`        | `main.py`, `run_daemon.py`                                                            |
| F-32  | [orch] `run_daemon.py --market-hours-only` cannot be disabled from CLI                       | `run_daemon.py`                                                                       |
| F-33  | [risk] Daily/weekly loss limits anchored to boot balance, not current equity                | `packages/core/risk_manager.py`                                                       |
| F-34  | [risk] Position sizing forces minimum 1 share when math says 0                              | `packages/core/risk_manager.py`                                                       |
| F-35  | [port] `Portfolio.get_performance_metrics()` returns `float("inf")` (B-15 residual)         | `packages/core/portfolio.py`                                                          |
| F-36  | [port] `open_position` swallows DB duplicate-check failures with bare except                | `packages/core/portfolio.py`                                                          |
| F-37  | [db]   `store_trade` idempotency is racy SELECT-then-INSERT (no UNIQUE)                      | `packages/core/database.py`                                                           |
| F-38  | [brk]  `AngelOneBroker.modify_order` ignores broker rejection status                         | `packages/brokers/angelone.py`                                                        |
| F-39  | [brk]  `get_funds` masks API failure as zero cash                                            | `packages/brokers/angelone.py`                                                        |
| F-40  | [exec] `ExecutionEngine` SL registry mutated from multiple threads without lock              | `packages/core/execution.py`                                                          |
| F-41  | [exec] Live flatten/exit closes portfolio at quote price, not broker-reported fill          | `packages/core/execution.py`                                                          |
| F-42  | [strat] LSTM lacks feature-count / artifact contract validation (vs XGBoost)                 | `packages/strategies/lstm_model.py`                                                  |
| F-43  | [strat] LSTM sequence inherits today's regime on prior-session timesteps                    | `packages/core/features.py`                                                           |
| F-44  | [strat] Day high/low window uses 78 bars; NSE session is 75 (~15m leak from prior session)  | `packages/core/features.py`                                                           |
| F-45  | [strat] ATR formula differs between FeatureEngine (EWM) and BaseStrategy (SMA)              | `packages/core/features.py`, `packages/strategies/base_strategy.py`                  |
| F-46  | [strat] VWAP bounce BUY/SELL use incompatible ATR + volume thresholds                       | `packages/strategies/vwap_bounce.py`                                                  |
| F-47  | [strat] VWAP bounce compares prev-bar VWAP across session boundary                          | `packages/strategies/vwap_bounce.py`                                                  |
| F-48  | [strat] Trend filter caches fetch failures for full 6h TTL → no retry                       | `packages/strategies/_trend_context.py`                                               |
| F-49  | [data] `on_candle_close` callback runs under aggregator lock incl. DB I/O                    | `packages/core/tick_aggregator.py`                                                    |
| F-50  | [data] `flush_all()` lacks exception guard around callback                                   | `packages/core/tick_aggregator.py`                                                    |
| F-51  | [data] Historical-cache filename ignores IST normalization → duplicate keys                  | `packages/core/historical_cache.py`                                                   |
| F-52  | [data] In-memory historical cache returns mutable DataFrame reference                       | `packages/core/data_handler.py`                                                       |
| F-53  | [data] Yahoo / AngelOne timestamps stored as timezone-naive UTC                             | `packages/core/data_handler.py`, `packages/core/stock_scanner.py`                    |
| F-54  | [data] No WebSocket tick-staleness watchdog                                                 | `packages/core/websocket_client.py`                                                  |
| F-55  | [data] `self_sufficiency._persist()` lacks `file_lock` (C-29 missed module)                  | `packages/core/self_sufficiency.py`                                                   |
| F-56  | [data] Self-sufficiency cost burn uses calendar days, not trading days                      | `packages/core/self_sufficiency.py`                                                   |
| F-57  | [alert] Dedup check + send + record not under same lock (TOCTOU duplicate sends)             | `packages/monitoring/alerts.py`                                                       |
| F-58  | [alert] Dedup state recorded only after successful send (crash window resends)              | `packages/monitoring/alerts.py`                                                       |
| F-59  | [alert] `drain_failed_alerts()` replays without dedup check                                 | `packages/monitoring/alerts.py`                                                       |
| F-60  | [alert] Failed-alert spool writes non-atomic                                                | `packages/monitoring/alerts.py`                                                       |
| F-61  | [stats] `_pnl_list` grows without bound per strategy                                         | `packages/core/trade_analyzer.py`                                                     |
| F-62  | [stats] NaN PnL corrupts profit factor + Sharpe accumulators                                | `packages/core/trade_analyzer.py`                                                     |
| F-63  | [ui]   Streamlit Today P&L + range cutoffs use host-local TZ                                 | `packages/monitoring/streamlit_app.py`                                                |
| F-64  | [train] Battery `paper_seed` field exists but never set by harness                          | `packages/research/battery.py`                                                        |
| F-65  | [train] Simple `BacktestEngine` iterates symbols sequentially, not chronologically          | `packages/research/backtest.py`                                                       |
| F-66  | [train] Simple `BacktestEngine` mark-to-market uses single-symbol price dict                 | `packages/research/backtest.py`                                                       |
| F-67  | [train] End-of-backtest flatten omits exit slippage                                          | `packages/research/backtest.py`, `packages/research/backtest_ensemble.py`             |
| F-68  | [train] LSTM training fills NaN features with zero                                          | `packages/training/train_lstm.py`                                                    |
| F-69  | [train] LSTM training has no RNG seed (non-reproducible)                                    | `packages/training/train_lstm.py`                                                    |
| F-70  | [train] `prepare_dataset` falls back to row-index split if timestamp sort fails             | `packages/training/prepare_dataset.py`                                               |
| F-71  | [train] Battery serial mode writes results JSON non-atomically                              | `packages/research/battery.py`                                                        |
| F-72  | [train] Expected-profit gate branch skips daily-equity update                                | `packages/research/backtest_ensemble.py`                                              |
| F-73  | [train] Per-trade payloads inflate every battery variant JSON (memory/disk bloat)            | `packages/research/battery.py`                                                        |
| F-74  | [tool] `trade_postmortem.py` yfinance calls have no timeout bound                           | `tools/trade_postmortem.py`                                                           |
| F-75  | [tool] `send_eod_summary.py` / `preview_eod_summary.py` use cwd-relative config             | `tools/send_eod_summary.py`, `tools/preview_eod_summary.py`                          |
| F-76  | [tool] `audit_checkpoint.py` writes checkpoint files without atomic replace                  | `tools/audit_checkpoint.py`                                                           |
| F-77  | [tool] `_restore_positions.py` mutates DB without auto-backup                                | `tools/_restore_positions.py`                                                         |
| F-78  | [tool] `run_daemon_resilient.ps1` hardcodes developer-specific Python path                  | `tools/run_daemon_resilient.ps1`                                                      |
| F-79  | [tool] `run_battery_queue.py` hardcodes Linux-only paths                                    | `tools/run_battery_queue.py`                                                          |
| F-80  | [tool] Stale deploy runbook still wires `healthcheck_cron.sh` instead of watchdog            | `docs/cloud_mvc_runbook.md`, `tools/cloud/healthcheck_cron.sh`                       |
| F-81  | [exec] `--reset-balance` with restored open positions leaves incoherent cash                | `packages/core/portfolio.py`                                                          |
| F-82  | [strat] Live regime per-bar injection for sequence models (LSTM)                            | `packages/core/features.py`                                                           |

## LOW

| ID    | Title                                                                                       | File                                                                                  |
| ----- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| F-83  | [strat] XGBoost ties (`prob_up == prob_down`) resolve to SELL, LSTM resolves to HOLD         | `packages/strategies/xgboost_classifier.py`                                          |
| F-84  | [strat] Ensemble `min_strategies_agree` counts raw votes not unique strategy names           | `packages/strategies/ensemble.py`                                                     |
| F-85  | [strat] OBV in FeatureEngine accumulates across sessions while VWAP resets daily             | `packages/core/features.py`                                                           |
| F-86  | [ui]   Streamlit market-open indicator ignores NSE holidays                                  | `packages/monitoring/streamlit_app.py`                                                |
| F-87  | [ui]   Streamlit risk tab still emits `float("inf")` profit factor                           | `packages/monitoring/streamlit_app.py`                                                |
| F-88  | [alert] Full alert bodies logged at INFO/WARN/ERROR                                          | `packages/monitoring/alerts.py`                                                       |
| F-89  | [data] Stock scanner extreme-RSI filter passes NaN RSI                                       | `packages/core/stock_scanner.py`                                                      |
| F-90  | [db]   `ticks` table lacks PK / UNIQUE → unbounded duplicate rows                            | `packages/core/database.py`                                                           |
| F-91  | [db]   `load_trades_for_day` uses unindexed `substr(exit_time,1,10)=?`                       | `packages/core/database.py`                                                           |
| F-92  | [safe] OHLCV staleness gate bypassed for bars older than 12 hours                            | `packages/core/market_safety.py`                                                      |
| F-93  | [chrg] Charge rate env override fails open on parse typo                                     | `packages/core/charges.py`                                                            |
| F-94  | [tool] `send_eod_summary.py` error text references wrong config keys                         | `tools/send_eod_summary.py`                                                           |
| F-95  | [tool] `_preflight.py` displays `backtest.initial_capital` (₹10k) instead of `capital.initial_balance` (₹100k) | `tools/_preflight.py`                                          |
| F-96  | [tool] B023 closure-over-loop-variable in `run_battery_queue._try_launch`                    | `tools/run_battery_queue.py`                                                          |
| F-97  | [tool] F841 `sl_distance_per_share` — incomplete NEAR-SL flag heuristic                      | `tools/trade_postmortem.py`                                                           |
| F-98  | [tool] F841 dead vars cleanup (`kept_trades`, `trade_re`, `marker`)                          | `tools/analyze_hourly_blackouts.py`, `_postmortem_aggregate.py`, `_meesho_postmortem.py` |
| F-99  | [tool] Config drift: `risk.entry_blackout_hours` referenced but not in config.yaml           | `tools/analyze_hourly_blackouts.py`                                                   |
| F-100 | [train] XGBoost training docstring claims time-series CV; no CV implemented                  | `packages/training/train_xgboost.py`                                                 |
| F-101 | [train] `pickle.load` on battery market_data cache without integrity check                   | `packages/research/battery.py`                                                        |
| F-102 | [train] Battery pinned `--run-id` without `--resume` mixes prior-run artifacts               | `packages/research/battery.py`                                                        |
| F-103 | [train] Simple-backtest `commission_pct` knob is dead (charges engine used instead)          | `packages/research/backtest.py`                                                       |
| F-104 | [train] `analyze_day.py` loads equity curve but never uses it (no daily Sharpe / MDD)        | `packages/research/analyze_day.py`                                                    |
| F-105 | [strat] Model deserialization has no integrity / allowlist enforcement (B-19 logged only)    | `packages/strategies/lstm_model.py`, `packages/strategies/xgboost_classifier.py`     |
| F-106 | [alert] Telegram referenced in audit scope but not implemented                               | `packages/monitoring/alerts.py`                                                       |
| F-107 | [train] LSTM no feature-count contract check — duplicate of F-42 for completeness            | covered by F-42                                                                       |
| F-108 | [train] `_state_check.py` dynamic SQL bandit B608 (false positive)                           | `tools/_state_check.py` (cosmetic `# nosec`)                                          |

## DEFERRED (require explicit user policy or major architectural design)

| ID     | Title                                                                                      | Reason                                                              |
| ------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| D-F-01 | True broker idempotency (client-order-id + orderBook reconciliation before retry)          | Requires AngelOne integration design + behaviour-changing for live |
| D-F-02 | F-03 `_shutdown` flatten contract — should SIGTERM also flatten?                            | Policy decision (operator UX)                                       |
| D-F-03 | F-07 JWT refresh + WS reconnect orchestration                                              | Requires controlled WS restart design                               |
| D-F-04 | F-21 LSTM cross-symbol sequence refactor                                                   | Large training-pipeline rewrite; affects future retrains only      |
| D-F-05 | F-37 DB UNIQUE index migration (`trades.symbol+exit_time`, `ticks` PK)                     | Schema change; needs migration script + idempotent backfill        |

---

## Plan summary

- **Tier A — fix now, no behaviour change on live trading**: ~45 items (operator tools, defensive guards, atomic writes, mutable-alias fixes, dead/wrong-config keys, atomic spool, NaN guards, lint cleanup, Streamlit TZ).
- **Tier B — fix now, behaviour-affecting correctness**: ~20 items (LSTM market-context wiring, NaN→HOLD, cumulative-volume fix, WS resubscribe, regime accumulator rehydrate, partial-fill exit symmetry, daily-loss-limit anchor, min-1-share force).
- **Tier C — backtest/training fidelity (no live impact)**: ~12 items (regime in backtest, paper-seed wiring, end-of-bt slippage, equity-update gate branch, time-split fallback, LSTM seed + NaN, XGBoost early-stop fold, etc.).
- **Tier D — deferred (5)**: documented above; require user input.

Slot accounting: behaviour-changing fixes will consume Slot #3 of 3 of FREEZE v2.1. Operator-tool / dead-code / defensive items are audit-only and do not consume a slot.
