# EOD Report — Wednesday, 2026-05-27 (IST)

_Operator narrative compiled from `logs/trading_agent_2026-05-27.log`, `logs/audit/2026-05-27/checkpoint_1545.md`, `logs/diagnostics/eod_2026-05-27.md` (auto-gen), `logs/signal_audit_2026-05-27.csv`, `logs/health.json`. Post-mortem absent (zero-trade day — `pull_logs.ps1` correctly skipped Optional path). Compiled 15:50 IST after market close._

---

## TL;DR (one line)

**₹0.00 realised — zero trades, by design. Both freeze-v2.1 slots in effect (slot-1 `allow_shorts: false` LIVE+DURABLE, slot-2 `xgboost_classifier` disabled LIVE since 11:25 IST). 29 SELL signals generated by `rsi_momentum`, every single one correctly blocked at the order-builder. Capital intact at ₹120,990. Cumulative since deployment day 1: -₹1,212.26 (no further bleed today). Operationally an extremely busy day — 20 commits, queue trimmed, perf-fix sprint shipped, backtester restarted on HEAD `84f5acd`.**

---

## 1. Trading session — what fired and what was blocked

### Signal generation (all from `rsi_momentum`)

| Metric | Value |
|:-------|:------|
| Total signals logged in `signal_audit` | **29** |
| Direction distribution | 29 SELL · 0 BUY |
| Strategy distribution | 29 `rsi_momentum` · 0 from `supertrend_follow` / `vwap_bounce` / `opening_range_breakout` / `xgboost_classifier` |
| Regime at signal time | 24 `bear_low_vol` · 5 `bear_high_vol` |
| First signal | 09:24:25 IST (PAYTM, SELL, conf=0.622) |
| Last signal | 15:14:42 IST (ICICIPRULI, SELL, conf=0.682) |
| **Outcome** | **29/29 REJECTED. Zero orders placed. Zero trades.** |

### Rejection-reason breakdown (the gates that did the work)

| Rejection reason | Count | Gate / source |
|:-----------------|:-----:|:--------------|
| `allow_shorts:false` (long-only mode) | **19** | **Slot-1 (LIVE+DURABLE, commit `8e1e926`)** |
| `opening_lockout` (first 15 min of session) | 6 | Pre-existing risk gate |
| `expected_profit:poor_rr` (R:R below 1.00× risk) | 4 | Pre-existing R:R gate on `rsi_momentum` |
| **Sum** | **29** | — |

### XGBoost firing pattern

| Window | BUY | SELL | HOLD | Source |
|:-------|:---:|:----:|:----:|:-------|
| Pre-11:25 IST (xgb still active) | 0 | 0 | 0 | model loaded but `strategies.active` filter not yet applied |
| Post-11:25 IST (slot-2 LIVE) | **0** | **0** | **0** | `xgboost_classifier` removed from `config.yaml:strategies.active` — never invoked |
| Day total | 0 | 0 | 0 | — |

After commit `f32009c` (11:25 IST) deployed and the daemon restarted (11:28 IST), the `XGBoost model loaded from models/xgboost_model.pkl` log line never appeared again — confirming the strategy is not even initialised, let alone called. **Slot-2 working perfectly.**

---

## 2. Why ZERO trades — both freeze slots functioning as designed

This was the **first full trading day** with both defensive freeze-v2.1 slots in steady state simultaneously. The arithmetic of why zero entries happened:

1. **No BUY signals from any strategy.** In a bear-low-vol / bear-high-vol regime, the four rule strategies in `strategies.active` (`mean_reversion`, `supertrend_follow`, `rsi_momentum`, `vwap_bounce`, `opening_range_breakout`) collectively produced **zero BUY votes** the ensemble could pass through. This is expected — none of the rule strategies are primarily long-side in bear regimes, and `xgboost_classifier` (the only model previously producing BUYs in this market) is disabled.
2. **All SELL signals were from `rsi_momentum`.** It is the only short-side momentum strategy currently active. 29 of its SELL signals reached the audit log.
3. **All 29 SELLs were killed before order placement.** 19 by slot-1's `[ALLOW-SHORTS] Skipping new SHORT for {SYM}: risk.allow_shorts=false` line, 6 by the opening-lockout gate (signals fired in the first 15 minutes of trading), 4 by the R:R gate (signal reward too small relative to risk).

**Net read:** the system is in the designed defensive posture. Yesterday lost ₹453 from 3 XGB BUY stop-outs in a bear market; today, with the cause of those losses removed (XGB disabled) and the other major bleed source disarmed (short-side `rsi_momentum` blocked), the result is ₹0 realised. Capital is being protected, not deployed.

This is the floor of the "phase-C monitor" plan (todo `phase_c_monitor`): minimal-to-zero entries until the retrained XGBoost model is qualified. Today is data point 1.

---

## 3. Operational events — extremely active day (20 commits, 0 trades)

A complete inversion of yesterday: zero trading action, intense codebase activity.

### Commit timeline (IST, all on `main`)

| Time (IST) | Commit | Subject | Class |
|:----------:|:-------|:--------|:------|
| 08:10 | `9b9b20c` | perf(backtester): Tier-D speed-up sweep + 2nd-pass business-logic fixes | (overnight) |
| 08:49 | `8c9bbbb` | perf(backtester): B-05 instantaneous-rate field in `BATTERY-PROGRESS` | (overnight) |
| 10:59 | `8e1e926` | **fix(config): commit `allow_shorts: false` to git (slot-1 durability fix)** | **slot-1 LIVE+DURABLE** |
| 11:00 | `e1df9e8` | feat(regime): per-cycle `[REGIME-INPUT]` observability log (diag-sprint H1) | audit-only |
| 11:13 | `35927ea` | docs+test(diag-sprint): findings_log + sprint plan + regime-log regression tests | audit-only |
| 11:17 | `8bcc360` | docs(freeze): reclassify slot-2 + slot-3 audit sweeps to audit-only (3/3 → 1/3) | audit-only |
| 11:25 | `f32009c` | **fix(strategies): DISABLE `xgboost_classifier` in live config (slot-2: broken model)** | **slot-2 LIVE** |
| 11:41 | `07e38c9` | docs(findings): forensic-audit synthesis + slot-2 + trades hygiene | audit-only |
| 11:43 | `a3de74d` | docs: Bug I archival — single-page diff summary + closure verdict | audit-only |
| 12:22 | `1fe1deb` | P-03: speed up SupertrendFollow (vectorise + ATR cache + drop `data.copy`) | audit-only (perf) |
| 12:22 | `7f19990` | P-04: drop unnecessary `data.copy()` in 5 rule-based strategies | audit-only (perf) |
| 12:23 | `0809cf5` | P-11: cache feature_cols + numpy handoff in `LSTMPriceModel` inference | audit-only (perf) |
| 12:25 | `9ac6435` | docs: log §7 perf sprint (P-03/P-04/P-11) in findings_log | audit-only |
| 13:36 | `68700a9` | tools(status): fix worker progress display in `battery_status_remote.ps1` | audit-only |
| 14:03 | `11eaf3d` | test(xgboost): invert config-enables guard to assert disabled-pending-retrain | audit-only |
| 14:09 | `e624e2d` | tools(status): derive `started` from `run_id`, not dir mtime | audit-only |
| 14:13 | `ae847e3` | docs(findings): honest attribution — xgb-disable dominates the 2-3x throughput, not perf fixes | audit-only |
| 15:41 | `84f5acd` | queue(battery): trim slots #3-6 — drop `v2_baseline_90d` / `v2_train_60d` / `v2_baseline_120d`, promote holdout to #3 | audit-only |
| 15:44 | `6816265` | docs(findings): log §8 — battery queue trim deploy audit | audit-only |

### Deploy actions to live infrastructure

| Time (IST) | Action | Outcome |
|:----------:|:-------|:--------|
| ~10:50 | Trader VM `git pull` + `docker compose up --build` — slot-1 durability + regime observability | Daemon restarted; `XGB-HEALTH OK 31 features` confirmed |
| ~11:25 | Trader VM `git pull` + rebuild — slot-2 (xgboost disable) deployed | Daemon restarted at 11:28; no more `XGBoost model loaded` log lines (strategy not in active list) |
| ~11:30 | Backtester VM scheduler stopped + worker container killed | `PAUSED_20260527T060131Z.md` marker left on VM |
| ~12:27 | Backtester VM cleanup + rebuild on HEAD `9ac6435` (perf-fix sprint live) | New `battery_nifty50_60d_20260527T065700` container spawned (UTC `065700` = IST 12:27) |
| ~13:30 | Backtester scheduler verified producing `BATTERY-PROGRESS` lines, ev/s in 75–104 range | Performance gain dominated by xgb-disable (~2×); P-03/P-04 contribute ~1.3–1.5× on top |
| 15:42 | Backtester VM `git pull` to HEAD `84f5acd` + `systemctl restart battery-scheduler.service` | Queue 6 jobs → 3 jobs; in-flight container untouched (`Up 3 hours`, same PID) |

### Trader daemon restart audit

Five daemon restarts observed in the morning, then steady state for the rest of the day:

```
08:00:17  Initial morning start (XGB-HEALTH OK)
10:40:42  Restart for slot-1 durability deploy (XGB-HEALTH OK)
10:44:31  Restart, likely re-deploy retry (XGB-HEALTH OK)
11:03:59  Restart for regime observability deploy (XGB-HEALTH OK)
11:28:00  Final restart for slot-2 (xgb disable) — NO XGB-HEALTH line afterwards
            └─ Uptime at 15:45 EOD checkpoint: 259 min (= continuous since 11:26 IST)
```

The absence of `XGB-HEALTH` lines after 11:28 IST is the live verification that `xgboost_classifier` is no longer being initialised — exactly the desired state after slot-2.

### Bypass-ledger / freeze-v2.1 status at EOD

- **Slot 1 — `risk.allow_shorts: false`** — **LIVE + DURABLE** (committed to git at `8e1e926`, no longer relying on filesystem hot-fix). 19/29 SELL rejections today attributed directly to this gate.
- **Slot 2 — `xgboost_classifier` removed from `strategies.active`** — **LIVE** (commit `f32009c`, 11:25 IST). Zero XGB firings today confirms model is not even loaded.
- **Slot 3 — reserved for model retrain.** Runbook documented in `findings_log_2026-05-27 §5.9`. Will be consumed when the retrained model qualifies through backtest + paper-validation.
- **Audit-only entries today (no slot consumed):** regime observability, perf sprint (P-03/P-04/P-11), test guards, queue trim, doc commits.
- **Bypass count: 2 of 3 slots used.** 1 remaining (reserved for slot-3 retrain).

---

## 4. Items the auto-EOD diagnostic flagged

From `logs/diagnostics/eod_2026-05-27.md` (analysed over last 7d, N=7 trades — all pre-slot-2):

- **Rolling 5-day PF = 0.09** — Phase-A verdict still **FAIL**. Trades in the window: 2026-05-18..2026-05-26. Today is 0 trades so it doesn't extend or reset the window. The number reflects the **pre-fix** distribution; we expect it to remain FAIL until the retrained model produces enough trades to refresh the window.
- **Portfolio Kelly = -2.227.** Math says do not bet at current point-estimate. With slot-1 + slot-2 LIVE, the system is now mechanically not betting — which is the right response.
- **Per-strategy:**
  - `rsi_momentum`: 4 trades, WR 50.0%, PF 0.32, -₹169 cumulative. All shorts. Status `INSUFFICIENT_DATA` (N<10).
  - `xgboost_classifier`: 3 trades, WR 0.0%, PF 0.00, -₹453 cumulative. All longs (yesterday's stop-outs). Status `INSUFFICIENT_DATA` (N<10). **No new entries possible** — strategy disabled.
- **Sector concentration:** N too small (1–2 per supersector) to draw conclusions. Largest single-sector loss: Infra (-₹240), Chemicals (-₹209) — both from yesterday's XGB stop-outs.
- **Long vs Short:** `rsi_momentum` longs N=0, shorts N=4. `xgboost_classifier` longs N=3, shorts N=0. This confirms the asymmetry slot-1 was designed to address — `rsi_momentum` was structurally short-only, slot-1 freezes its entry channel without killing the strategy.

### Self-sufficiency

- **State: YELLOW** (unchanged from yesterday).
- Cumulative realised since deployment (day 13): **-₹1,212.26**.
- Cost burn-to-date: **₹1,625.00** (₹125/trading-day, ₹2,500/month).
- Coverage: **-75%**.
- **Today contributed ₹0 to either side** — no bleed, no recovery. Holding pattern.

---

## 5. Backtester evidence stream — slot #1 in flight

The backtester is producing the parallel evidence stream that will inform the Friday 2026-05-29 review. Status at EOD:

| Slot | Job | Variants done | ETA (IST) |
|:----:|:----|:--------------|:----------|
| 1 | `nifty50_60d` (RUNNING) | 4 of 19 (V1, V2, V3, V4) | ~19:30 |
| 2 | `nifty500_v4_long_only_validation_60d` | (queued) | Thu 07:00 |
| 3 | `v2_holdout_30d` *(promoted from old slot #5)* | (queued) | Thu 23:00 |

### Initial variant rankings (4 of 19, on 4-strategy no-xgb ensemble)

| Variant | Trades | WR% | PnL | PF | Notes |
|:--------|:------:|:---:|:----:|:----:|:------|
| V1 `baseline_current_shipped` | 29 | 41.4 | -₹67 | 0.76 | Current production config |
| V2 `all_filters_off` | 30 | 36.7 | -₹128 | 0.58 | All trend filters off — worst |
| V3 `only_xgb_mr_filtered_yday` | 30 | 36.7 | -₹128 | 0.58 | Byte-identical to V2 (filters target inactive strategies) |
| V4 `threshold_3pct` | 27 | 40.7 | **-₹44** | **0.85** | **Best so far** — 3% trend filter trims weakest entries |

V4 is currently the most promising — fewer trades, smaller loss, higher PF. Whether it survives slot #3's holdout-30d will determine if the 3% threshold is a real edge or overfit to slot #1's 60d window.

### Throughput notes (honest attribution)

End-to-end backtester throughput jumped from 19–40 ev/s pre-pause to 75–104 ev/s post-restart. **The bulk of that ~2.5–3× speedup is xgboost-disable** (no more `FeatureEngine.compute_all + xgb.predict` per event), **not** the perf-fix sprint. The perf fixes (P-03/P-04/P-11) contribute roughly ~1.3–1.5× on top — real but smaller than I initially implied. See `findings_log_2026-05-27 §7.5` for the honest decomposition.

### Queue trim

Pre-EOD, the 6-job queue was trimmed to 3 jobs (commit `84f5acd`, deployed 15:42 IST). Net compute saved: ~144h ≈ 6 days. The dropped jobs (`v2_baseline_90d`, `v2_train_60d`, `v2_baseline_120d`) were designed to characterise the SHIPPED 5-strategy ensemble; with slot-2 LIVE, they would have re-validated a 4-strategy ensemble already strictly dominated by the upcoming post-retrain ensemble. Preserved in git at `HEAD~1` for one-command rollback. See `findings_log_2026-05-27 §8`.

---

## 6. State going into 2026-05-28

| Component | State at EOD | Notes |
|:----------|:-------------|:------|
| Trader daemon | **Up (healthy)** — PID 7, 188 MB, **259 min uptime** since 11:28 IST | Continuous since slot-2 deploy |
| Trader code | `73c26bf` + the 2 morning hot-fix commits merged | Operator-rebuilt yesterday; verified clean today |
| Trader config (in container) | `allow_shorts=False`, `xgboost_classifier` NOT in `strategies.active` | Verified via `signal_audit` (zero XGB rows) + `[ALLOW-SHORTS]` rejections (19) |
| Backtester daemon | **Scheduler active** (PID 1050628, restarted 15:42 IST); **container in-flight** (`battery_nifty50_60d_20260527T065700`, Up 3h) | New 3-job queue loaded |
| Backtester code | HEAD `84f5acd` (perf fixes + queue trim) | Verified |
| Cash | **₹120,990.17** | Unchanged from yesterday's close |
| Open positions | **0** | All flat |
| Cooldowns | (none added today) | Yesterday's TATAINVEST/HFCL/TATACHEM cooldowns expired at day boundary |
| Risk state | NORMAL, drawdown 1.63% | Well within 20% halt threshold |
| Trading blocked | **NO** (consec losses reset to 0) | Yesterday's `ConsecLoss=3` block cleared at 08:01 IST daily reset |
| `risk.allow_shorts` | **false** (LIVE+DURABLE in git) | Will persist across any restart/redeploy |
| `xgboost_classifier` | **DISABLED** (commented out of `strategies.active` in git) | Will persist across any restart/redeploy |
| Alert pipeline | OK | EOD diagnostic dispatched (post-mortem suppressed — zero trades) |

### Expected tomorrow morning (2026-05-28, ~08:00 IST)

- Daily trackers reset → no inherited blocks (today had no blocks anyway).
- Cooldowns: none to expire.
- **Active strategies** (4): `mean_reversion`, `supertrend_follow`, `rsi_momentum`, `vwap_bounce`, `opening_range_breakout`. **`xgboost_classifier` will NOT load** (not in `strategies.active`).
- `rsi_momentum` shorts will continue to be rejected by slot-1 (`allow_shorts=false`).
- **Expected trade activity: 0–2 trades** — same as today, unless one of the rule strategies happens to produce a strong long-side BUY in a regime where the trend filter and confidence threshold both clear. With Nifty likely still below 200 EMA, this remains low-probability.

---

## 7. Friday review (2026-05-29) — what we need from the backtester

The Friday review decisions are now informed by **two evidence streams** plus today's live observation:

1. **Live observation today:** the gates work. Slot-1 cleanly blocks all rsi_momentum shorts. Slot-2 cleanly removes the broken-XGB bleed. Zero capital impact. **This validates the cheapest possible defensive posture — there is no rush to deploy a riskier change before the review.**
2. **Backtester slot #1 (nifty50_60d):** all 19 variants complete by ~19:30 tonight on the 4-strategy ensemble. Will give a variant ranking on the 50-stock universe.
3. **Backtester slot #2 (validation_60d):** finishes Thu morning. 6 targeted variants (V1, V2, V4, V17, V18, V19) on the full 232-stock universe — answers whether V4's 3% threshold and the long-only configs (V17/V18/V19) transfer from large-caps to the broader universe.
4. **Backtester slot #3 (v2_holdout_30d):** finishes Thu late evening. All 19 variants on the held-out last 30d of a 90d window — the p-hack guard.

If V4 (or V18, the long-only variant of V4) survives all three jobs with positive PF, that becomes the next live config candidate post-retrain. The retrain itself (slot-3) is the second deliverable for the Friday review.

---

## 8. Open follow-ups (for Thursday 2026-05-28)

1. **Verify daemon morning startup** — at ~08:00 IST, confirm `[ALLOW-SHORTS]` rejections are firing and that no `XGBoost model loaded` line appears (slot-2 still in effect across the daily reset).
2. **Monitor backtester slot #1 completion** (~19:30 IST tonight → results visible Thursday AM). Spot-check that V5–V19 don't produce wildly different numbers from V1–V4 (catastrophic-divergence guard).
3. **Backtester slot #2 spawn** (Thu morning, automatic via scheduler) — log the spawn-time and the first variant's first BATTERY-PROGRESS line to confirm the 232-stock universe loads correctly with the bind-mounted model.
4. **Trader VM `trades.csv` cleanup** — after-hours task: archive the 38 `manual_test` rows from `/opt/trading-agent/logs/trades.csv` (pending todo `trader_vm_trades_csv_cleanup`).
5. **Bug J permanent fix** — `tools/cloud/bootstrap_backtester.sh` chown fix is still queued for sprint Day 4. Not blocking, but worth landing while the backtester is producing useful work in the background.
6. **No new bypass slots to consume.** 1 of 3 remaining, reserved for the retrain (slot-3).

---

_Compiled by trading-agent operator audit pass. Cross-references: auto profit-diagnostic at `logs/diagnostics/eod_2026-05-27.md`, audit checkpoints at `logs/audit/2026-05-27/checkpoint_*.md` (20 hourly checkpoints), signal_audit at `logs/signal_audit_2026-05-27.csv` (29 rows, all REJECTED), findings log §5–§8 in `docs/findings/findings_log_2026-05-27.md`, diagnosis sprint in `docs/diagnoses/diagnosis_sprint_2026-05-27.md`, freeze policy slot ledger in `docs/freeze/FREEZE_v2.1.md`, queue config in `data/battery_queue.yaml` at commit `84f5acd`._
